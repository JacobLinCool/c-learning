// Copyright (c) JacobLinCool
#include <stdio.h>
#include <stdint.h>
#include <inttypes.h>
#include <stdlib.h>
#include <string.h>

/**
 * @brief Re-allocate memory size.
 *
 * @param **pptr the original pointer.
 * @param before the original size.
 * @param after the wanted size.
 */
void my_realloc(void** pptr, size_t before, size_t after);

// Any application that can be written in JavaScript, will eventually be written in JavaScript.
